<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>

<body>
    <div class="w-full p-10 min-h-screen bg-zinc-800 text-white">

        <div class="w-full flex  justify-end">
            <a href="/logout"
                class=" px-6 py-2 border-[1px] border-zinc-300 bg-red-500 rounded-lg font-bold text-lg ">Logged Out</a>
        </div>
        <div class="flex items-start gap-3">
            <div class="w-10 h-10 rounded-full overflow-hidden">
                <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilePic %>" alt="">
            </div>
            <h3 class="text-3xl mb-5 "><span class="font-light">Hello,</span>
                <%= user.username %> 👋
            </h3>
        </div>
        <h5 class="my-3">You can create a new post.</h5>
        <form action="/post" method="post">
            <textarea name="content" placeholder="What's on your mind"
                class="w-1/3 p-3 resize-none bg-transparent border-2 border-zinc-300 outline-none rounded block mb-5"
                id=""></textarea>
            <input type="submit" name="" id=""
                class="px-6 py-2 border-[1px] border-zinc-300 bg-zinc-500 rounded-lg font-bold text-lg  block">
        </form>

        <div class="post mt-2">
            <h5 class="text-zinc-300">Your post</h5>

            <div class="postContainer mt-5">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="w-1/3 p-4 mb-4 rounded-md border-[1px] flex bg-zinc-700 flex-col">
                        <h1 class="text-blue-500 mb-2">
                            <%= user.username %>
                        </h1>
                        <p class="text-sm tracking-tight">
                            <%= post.content %>
                        </p>
                        <small class="mt-2 inline-block">
                            <%= post.likes.length %> likes
                        </small>
                        <div class="flex gap-2 mt-5">
                            <a class="text-blue-500 border-r-[1px] border-zinc-300 pr-2" href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id)===-1 ? "Like" : "Unlike" %>
                            </a>
                            <a class="text-zinc-400" href="/edit/<%= post._id %>">Edit</a>
                        </div>
                    </div>
                    <% }) %>
            </div>
        </div>
    </div>
</body>

</html>