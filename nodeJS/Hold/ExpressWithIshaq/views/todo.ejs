<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="/javaScript/app.js" defer></script>
</head>

<body>
    <div>
        <ul>
            <% for (var i=0; i < todo.length; i++) { %>
                <li>
                    <input 
                        type="checkbox" 
                        class="todo-checkbox" 
                        data-index="<%= i %>" 
                        <% if (todo[i].done) { %> checked <% } %>
                    >

                    <%= todo[i].name %>
                </li>
            <% } %>
        </ul>
    </div>
</body>

</html>